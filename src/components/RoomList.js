import React from 'react';
import PropTypes from 'prop-types';

const RoomList = props => {
  return (
    <div>
      This is room list
    </div>
  );
}

RoomList.propTypes = {

};

export default RoomList;
